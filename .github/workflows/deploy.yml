name: Deploy to GitHub Pages (DISABLED - Using Railway)

# DISABLED: This project uses Railway for hosting, not GitHub Pages
# on:
#   push:
#     branches: [ main ]
#   pull_request:
#     branches: [ main ]

# DISABLED: All jobs commented out since we use Railway for hosting
# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     
#     steps:
#     - uses: actions/checkout@v3
#     
#     - name: Setup Node.js
#       uses: actions/setup-node@v3
#       with:
#         node-version: '18'
#         
#     - name: Install dependencies
#       run: npm install
#       
#     - name: Build project
#       run: |
#         # Copy GitHub Pages compatible index file
#         cp index-github.html index.html
#         
#     - name: Deploy to GitHub Pages
#       uses: peaceiris/actions-gh-pages@v3
#       if: github.ref == 'refs/heads/main'
#       with:
#         github_token: ${{ secrets.GITHUB_TOKEN }}
#         publish_dir: .
#         publish_branch: gh-pages